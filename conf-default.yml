fromAddr: 

authentik:
  # do not include "https".
  url: 
  key: 

twingate:
  use: 
  # just the subdomain, as in "example" from "example.twingate.com"
  network: 
  key: 

mailtrap:
  # only SMTP with API key-based authentication is supported
  key: 

formats:
  # valid part forms: full, initial, or [nothing]
  username:
    separator: ''

    first: 
      form: initial
      order: 0

    middle: 
      form: 
      order: 

    last: 
      form: full
      order: 1